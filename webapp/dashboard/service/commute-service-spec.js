describe('commuteService', function() {

  beforeEach(module('dashboard'));

  it('should ...', inject(function(commuteService) {

	//expect(commuteService.doSomething()).toEqual('something');

  }));

});